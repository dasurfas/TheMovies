<UserControl x:Class="TheMovies.UI.Views.FilmoversigtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheMovies.UI.Views"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<CollectionViewSource x:Key="SorteredeFilm" Source="{Binding FilmCollection}">
			<CollectionViewSource.SortDescriptions>
				<scm:SortDescription PropertyName="Titel"/>
			</CollectionViewSource.SortDescriptions>
		</CollectionViewSource>
	</UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Filmoversigt" 
                   FontSize="24"
                   FontWeight="Bold" 
                   Margin="0,0,0,0"/>

        <TextBlock Grid.Row="1" 
                   Text="Vælg en film:" 
                   FontSize="14" 
                   Margin="0,0,0,0"/>

		<DataGrid Grid.Row="2"
		   ItemsSource="{Binding Source={StaticResource SorteredeFilm}}"
		   SelectedItem="{Binding ValgtFilm}"
		   AutoGenerateColumns="False"
				  BorderBrush="LightGray"
				  BorderThickness="1"
		   IsReadOnly="True"
		   HeadersVisibility="Column">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Titel" Binding="{Binding Titel}" Width="*" MinWidth="150"/>
				<DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="120"/>
				<DataGridTextColumn Header="Varighed (min)" Binding="{Binding Varighed}" Width="100"/>
				<DataGridTextColumn Header="Instruktør" Binding="{Binding Instruktør}" Width="150"/>
				<DataGridTextColumn Header="Premiere" Binding="{Binding PremiereDato, StringFormat=dd/MM/yyyy}" Width="120"/>
			</DataGrid.Columns>
		</DataGrid>


    </Grid>
</UserControl>
